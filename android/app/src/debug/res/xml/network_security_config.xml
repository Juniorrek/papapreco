<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: permits cleartext HTTP to the addresses a local docker compose
  API is reachable at from an emulator or the host. Lives under src/debug, so
  it never merges into a release build — that build keeps the platform
  default, which blocks cleartext HTTP entirely.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
